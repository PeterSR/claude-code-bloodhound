# Local scratch — never tracked
.agent-workspace/

# This repo's own project config: dogfooding, not a checked-in example.
# .bloodhound/config.json is designed to be committable, but the copy here
# is whatever the current session wants to hear, not a recommendation.
/.bloodhound/

# Go build artifacts
/bloodhound
/bloodhound.exe
/bloodhound-gui
/bloodhound-gui.exe
/dist/
/vendor/

# Frontend
/web/node_modules/
/web/.vite/
/web/*.tsbuildinfo

# Frontend build output: never tracked. //go:embed lives behind a `prod`
# build tag (see web/embed_prod.go); dev builds use the stub at
# web/embed_stub.go and serve a placeholder page. Production builds via
# `make build` populate web/dist before invoking go build.
/web/dist/

# Editors
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

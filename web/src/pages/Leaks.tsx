import ComingSoon from '../components/ComingSoon';

export default function Leaks() {
  return (
    <ComingSoon
      title="Leaks"
      description="Ranked list of avoidable spend by cause: idle cache misses, cold compactions, breakpoint rotations. Tells you whether to change behaviour (reply faster) or workflow (compact warm, not cold)."
    />
  );
}

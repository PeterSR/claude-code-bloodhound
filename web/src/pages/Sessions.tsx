import ComingSoon from '../components/ComingSoon';

export default function Sessions() {
  return (
    <ComingSoon
      title="Sessions"
      description="Sortable list of every Claude Code session on this machine, with click-through to a per-session timeline."
    />
  );
}
